mkdir -p .github/workflows

cat > .github/workflows/ping.yml <<'YAML'
name: ping
on:
  workflow_dispatch:
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - run: echo "ok"
YAML

git add .github/workflows/ping.yml
git commit -m "ci: add minimal ping workflow"
git push origin main
