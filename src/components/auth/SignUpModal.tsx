import React from "react";

/**
 * Stub do modal legado — não renderiza nada.
 * Mantém compatibilidade com imports existentes sem alterar estética.
 */
export default function SignUpModal() {
  return null;
}
