# ============================================================================
# Revo ERP — .env.example (SEM SEGREDOS / SEM VALORES)
# ============================================================================
# Este arquivo existe para documentar NOMES de env vars e seus usos.
# Valores devem ser configurados em:
# - local: `.env.local` (não versionado)
# - CI: GitHub Secrets
# - deploy: Netlify/Vercel env vars
#
# Regras:
# - nunca comitar valores reais (tokens/keys/passwords)
# - use placeholders vazios

# Frontend (Vite)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
VITE_SUPABASE_FUNCTIONS_URL=
VITE_SITE_URL=
VITE_SENTRY_DSN=

# Local dev (billing bypass / plano)
VITE_LOCAL_BILLING_BYPASS=
VITE_LOCAL_PLAN_SLUG=

# Operador (modo dev)
VITE_OPERATOR_DEV_PIN=

# Seed (dev)
VITE_ENABLE_SEED=

# Sentry build plugin (CI/build)
SENTRY_AUTH_TOKEN=
SENTRY_ORG=
SENTRY_PROJECT=

# Playwright / E2E
PLAYWRIGHT_BASE_URL=
E2E_USER=
E2E_PASS=
E2E_PLAN=

# ============================================================================
# Supabase Edge Functions (somente nomes; use Supabase Secrets em produção)
# ============================================================================
# Quando rodar funções localmente (ex.: `supabase functions serve`), pode usar env vars.
# Em PROD/DEV (Supabase hospedado), configure via Dashboard → Edge Functions → Secrets.

# Billing / Stripe
STRIPE_SECRET_KEY_TEST=
STRIPE_WEBHOOK_SECRET_TEST=
STRIPE_SECRET_KEY_PROD=
STRIPE_WEBHOOK_SECRET_PROD=
BILLING_TRIAL_DAYS=

# CORS
ALLOWED_ORIGINS=
ALLOW_ALL_ORIGINS=

# GitHub dispatch (backup/restore/tickets)
GITHUB_TOKEN=
GITHUB_REPO=
GITHUB_DEFAULT_REF=
GITHUB_API_BASE_URL=

# Cloudflare R2 (backups)
R2_ENDPOINT=
R2_BUCKET=
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
